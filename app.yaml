application: infowebviewer
version: 0-1
runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: /static
  static_dir: static

- url: .*
  script: app.py

libraries:
- name: jinja2
  version: latest
- name: re
  version: latest
