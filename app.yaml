application: infowebviewer
version: 0-1
runtime: python3
api_version: 1
threadsafe: true

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: app.application

libraries:
- name: jinja2
  version: latest
